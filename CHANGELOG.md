# 更新日志

本项目遵循语义化版本（SemVer）。

## v0.3.1 — 2026-07-11

### ✨ 新功能

- **行内 Git Blame（IDEA 风格 Annotate）**：在「全部文件」的文件内容视图右键行号区，选择「显示提交信息」，行号旁即显示每行的 `短哈希 + 作者` 注释；**点击注释直接跳转** Git 面板日志中对应的提交（自动打开面板、加载历史、选中并居中该行）。切换文件或编辑内容后注释自动关闭；未提交的行显示「未提交」。
- **「最近打开」悬停展开**：文件菜单中的「最近打开」子菜单悬停即弹出，无需点击，与原生菜单行为一致。
- **代码行高设置**：设置中新增行高滑块（1.2–1.8 倍），可按喜好调节编辑器行距。

### 🔧 优化

- **代码字体渲染**：为所有内置代码字体补齐真实粗体/斜体字面（此前粗体由浏览器合成，宽度失真导致字符错位、光标漂移）；启用编程连字（`=>`、`===` 等）；强制 WebView2 开启 LCD/ClearType 子像素文本渲染；关闭毛玻璃时使用不透明窗口背景，文字更锐利。
- **拖动光标重绘**：分隔条拖动光标改为「分隔条 + 实心箭头」的系统光标风格，深浅背景下都更清晰。
- **比较界面滚动条**：Monaco 编辑器滚动条与全局细滚动条统一（可见 4px，悬停加粗），不再显得过粗。
- **Git 日志界面细节**：搜索框与日志表格对齐并改为直角、收窄两侧边距；分支筛选输入框补上内边距。

### 📦 下载

下载对应平台的安装包：

- **Windows**：`.exe`（NSIS，一键用户级安装）或 `.msi`
- **macOS**：`.dmg`（Apple Silicon 选 aarch64，Intel 选 x64）
- **Linux**：`.deb` / `.AppImage` / `.rpm`

旧版本将通过内置更新器自动收到此次更新。

## v0.3.0 — 2026-07-10

本次是一次大版本升级：GitPrism 从只读的更改查看器进化为完整的 IDEA 风格 Git 客户端。

### ✨ 新功能

- **完整 Git 客户端**：暂存 / 取消暂存（支持按文件、按代码块、全部）、提交（支持 amend）、分支的新建 / 切换 / 删除 / 重命名 / 合并、fetch / pull / push、cherry-pick / revert / reset，以及**合并冲突解决器**（逐冲突块选择「我方 / 对方 / 两者」）。在外部终端执行的操作（如 `git merge --abort`）也会自动刷新界面。
- **IDEA 风格工具窗口布局**：左侧 ActivityBar 切换「项目 / 提交」面板，再次点击可折叠让出空间；Git 面板（日志 / 控制台）停靠底部，可调整大小。
- **合并式标题栏**：自定义标题栏将菜单（文件 / 视图 / 首选项）与窗口控制按钮合为一行，风格与 IDEA 一致。
- **Git 日志全面升级**：提交图（按分支泳道着色）、分支 / 标签徽章、IDEA 风格紧凑表格；左侧分支列表单击过滤日志、双击检出；支持全历史搜索（标题 / 作者 / 邮箱 / 哈希），右键可选显示哈希 / 邮箱 / 改动行数列；右键菜单新增撤销最后一次提交、丢弃提交、编辑 HEAD 提交消息，以及 IDEA 风格的「重置到此处」对话框。
- **克隆仓库**：支持单分支与浅克隆（深度）选项。
- **Pull 支持 merge / rebase**：可选择并持久化；rebase 冲突同样接入冲突解决器，支持继续 / 中止。
- **Git 控制台**：实时记录实际执行的 git 命令，子命令语法高亮。
- **仓库语言统计**：摘要视图展示 GitHub 风格的语言分布（按行数 / 文件数）。
- **可编辑工作区文件**：「全部文件」的单文件视图可直接编辑，`Ctrl+S` 或「保存」按钮写回磁盘（diff 视图保持只读）。
- **项目面板新建文件 / 文件夹**；Eclipse 键位：`Ctrl+W` / `F4` 关闭标签、`Ctrl+Shift+W` 关闭全部、`Ctrl+PageUp/PageDown` 切换标签。

### 🔧 优化

- **网络操作防挂起**：fetch / pull / push / clone 超时后会终止整个进程树，不再因失效连接永久卡住。
- **fetch / pull 实时进度**：直接流式显示 git 自身的进度输出，而非静态的「正在 fetch…」。
- 更细的滚动条、更紧凑的编辑器行高与行号槽；统一各处拖拽手柄样式与自定义调整光标。

### 🐞 修复

- Git 日志表头与内容列错位（未考虑滚动条占位）。
- 项目树「定位」按钮失效（选择器冲突）。
- 未选中文件时错误显示「修改前 / 修改后」标签。
- diff 视图两侧统一禁用自动换行（此前只有一侧生效）。

### 📦 下载

下载对应平台的安装包：

- **Windows**：`.exe`（NSIS，一键用户级安装）或 `.msi`
- **macOS**：`.dmg`（Apple Silicon 选 aarch64，Intel 选 x64）
- **Linux**：`.deb` / `.AppImage` / `.rpm`

旧版本将通过内置更新器自动收到此次更新。

## v0.2.3 — 2026-07-05

### 🐞 修复

- **多项目侧边栏重叠**：修复同时打开两个及以上项目时，展开一个项目的文件树会与另一个项目的标题栏视觉重叠的问题。根因是 v0.2.2「左侧导航重复滚动条」改动引入的单一滚动容器 + FLIP 动画结构；本次改回每个项目文件树各自独立滚动。副作用：拖拽项目排序时不再有滑动动画（排序功能本身不受影响）。

## v0.2.2 — 2026-06-28

本次发布带来若干交互细节优化与体验修复。

### ✨ 新功能

- **更新进度提示**：检查更新 / 下载更新时显示进度，过程更直观。
- **右击项目头打开所在目录**：在侧边栏右击项目标题，可在系统文件管理器中打开该项目文件夹。
- **显示隐藏 / 被忽略的文件**：设置中新增「显示隐藏的文件和文件夹」开关，「全部文件」视图可一并列出 `.gitignore` 忽略的文件与文件夹（如 `node_modules`、构建产物）。

### 🔧 优化

- **切换分支实时刷新**：在外部切换分支或变更 HEAD 后，侧边栏分支名与状态会随刷新实时更新，无需重新打开项目。
- **「最近打开」下拉美化**：列表项改为「项目名 + 灰色父目录路径 + 📁 图标」两行布局，触发按钮配 🕘 图标与可旋转的箭头；下拉改用更通透的磨砂材质，并移至 `<body>` 渲染以正确透出亚克力效果。

### 🐞 修复

- **左侧导航重复滚动条**：项目文件较多时不再出现内外两条滚动条，整个侧边栏作为单一滚动区域。

### 📦 下载

下载对应平台的安装包：

- **Windows**：`.exe`（NSIS，一键用户级安装）或 `.msi`
- **macOS**：`.dmg`（Apple Silicon 选 aarch64，Intel 选 x64）
- **Linux**：`.deb` / `.AppImage` / `.rpm`

旧版本将通过内置更新器自动收到此次更新。

## v0.2.1 — 2026-06-24

### ✨ 新功能

- **文件右键菜单**：在文件列表中右击任一文件，可「打开所在目录」（在系统文件管理器中定位）或「查看文件信息」。菜单会自动回拉进视口，避免在列表底部被截断。
- **文件信息弹窗**：展示文件的类型、相对路径与完整路径、大小、行数、修改/创建时间，以及只读、符号链接等属性；已删除的文件也能显示路径并提示「文件不存在」。

### 📦 下载

下载对应平台的安装包：

- **Windows**：`.exe`（NSIS，一键用户级安装）或 `.msi`
- **macOS**：`.dmg`（Apple Silicon 选 aarch64，Intel 选 x64）
- **Linux**：`.deb` / `.AppImage` / `.rpm`

旧版本将通过内置更新器自动收到此次更新。

## v0.2.0 — 2026-06-16

本次发布带来矢量图片预览、全新的毛玻璃界面，以及一批交互与可用性改进。

### ✨ 新功能

- **矢量图片预览**：支持 SVG 与 **Android Vector Drawable**（`<vector>` XML，含路径、分组、变换、裁剪、渐变）。在「全部文件」视图中渲染预览，并提供「预览 / 源码」切换；iOS/Android 资源都适用。
- **毛玻璃界面**：基于 Windows 11 acrylic 窗口材质，工具栏、侧边栏、标签栏、编辑区、菜单与各类弹窗均为半透明磨砂效果。
  - 设置中可一键**开关**毛玻璃；
  - 提供**透明度滑块**实时调节（分档保留层次：代码区最实、侧栏最透）；
  - 设置持久化，重启后保留。
- **文件列表多选 + 批量操作**：支持 `Ctrl/⌘` 点选、`Shift` 范围选择；选中多个改动文件后，底部浮出操作条可**批量「还原所选」**。
- **双击 Shift 打开文件搜索**（IDEA 风格）。

### 🔧 优化

- **视图模式驱动 diff/内容**：「更改」标签显示差异，「全部文件」标签显示文件内容（不再因文件有改动而强制显示 diff）。切换标签即时生效。
- **窗口自适应屏幕**：启动时按屏幕比例设置窗口大小并居中，高分辨率屏幕上不再显得过小。
- **最近打开**：原生下拉替换为自定义磨砂下拉，路径完整显示、外观统一。
- **编辑器滚动条自动隐藏**（悬停才显示，与整体一致）。
- **屏蔽浏览器默认行为**：右键原生菜单（输入框内保留以便粘贴）、打印 / 保存网页 / 查看源码 / 下载等快捷键。
- 移除「新窗口」按钮。

### 🐞 修复

- 修复右击最右侧标签时上下文菜单被窗口边缘截断的问题（自动回拉进视口）。
- 修复代码滚动时偶现的一根蓝色描边线（焦点描边漏入编辑器）。
- 统一所有 Monaco 主题下的代码选中色，并主题化应用内文字选中色（不再是系统默认刺眼蓝）。
- 让只读编辑器的光标正常闪烁。

### 📦 下载

下载对应平台的安装包：

- **Windows**：`.exe`（NSIS，一键用户级安装）或 `.msi`
- **macOS**：`.dmg`（Apple Silicon 选 aarch64，Intel 选 x64）
- **Linux**：`.deb` / `.AppImage` / `.rpm`

旧版本将通过内置更新器自动收到此次更新。
